name: stagex-build

concurrency:
  cancel-in-progress: true
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}"

on:
  push:
    tags:
      - "*.*.*"
      - "v*.*.*"
    branches:
      - main
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
      - "cmd/**"
      - "Containerfile"
      - "Makefile"
      - "go.mod"
      - "go.sum"
  pull_request:
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
      - "cmd/**"
      - "Containerfile"
      - "Makefile"
      - "go.mod"
      - "go.sum"
  workflow_dispatch:

jobs:
  build:
    name: Build image
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
    env:
      IMAGE_NAME: cluster-autoscaler-provider
      IMAGE_REPO: ghcr.io/${{ github.repository_owner }}/cluster-autoscaler-provider
      TAGS: >-
        ${{ github.ref == format('refs/heads/{0}', 'main') && 'latest' || '' }}
        ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.number) || '' }}
        ${{ github.event_name != 'pull_request' && github.ref_name || '' }}
      RELEASE: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
    steps:
      - name: Checkout sources
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 # v3.11.1

      - name: Build ${{ env.IMAGE_NAME }}
        uses: ./.github/actions/build-oci-package
        with:
          name: ${{ env.IMAGE_NAME }}
          version: ${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
          disable-artifact: true
          load-docker: true

      - name: Upload to GHCR
        if: ${{ env.TAGS != '' }}
        run: |
          for tag in ${TAGS}; do
            docker tag "tkhq/${IMAGE_NAME}:latest" "${IMAGE_REPO}:${tag}"
          done
          docker image push --all-tags "${IMAGE_REPO}"
